#vectordbsuffix: ollama
debug: true
vectordbsuffix: openai
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres
  config:
    import: "optional:classpath:/creds.yaml"
  ai:
    vectorstore:
      pgvector:
        index-type: HNSW
        distance-type: COSINE_DISTANCE
        dimensions: 384

---
spring:
  config:
    activate:
      on-profile: init
  ai:
    vectorstore:
      pgvector:
        remove-existing-vector-store-table: true
---
---
spring:
  config:
    activate:
      on-profile: cloud
  datasource:
    url: jdbc:postgresql://10.0.8.67:5432/postgres
    username: pgadmin
    password: WB084hGL3cH6yC2z7951
---
